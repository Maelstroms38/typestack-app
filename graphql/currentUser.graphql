query CurrentUser {
  currentUser {
    id
    username
    email
    places {
      id
      title
      description
      imageUrl
      user {
        id
        username
        email
      }
    }
  }
}
