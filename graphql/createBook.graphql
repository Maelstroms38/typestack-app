mutation CreateBook(
  $title: String!
  $summary: String!
  $isbn: String!
  $language: String!
  $author: String!
  $image: String!
  $genres: [String],
) {
  createBook(
    input: {
      title: $title
      summary: $summary
      isbn: $isbn
      image: $image
      language: $language
      author: $author
      genres:$genres
    }
  ) {
    book {
      title
      author {
        firstName
        lastName
      }
      image
    }
  }
}
